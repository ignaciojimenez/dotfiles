name: lint

on:
  push:
    branches: [master]
  pull_request:

# Read-only: every job only checks out and inspects the tree.
permissions:
  contents: read

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Run shellcheck
        # SC1090/SC1091: dynamic source paths (~/.foo, $SCRIPT_DIR/...) — by design.
        # SC2155: declare-and-assign on the same line — a stylistic warning we tolerate.
        run: |
          shopt -s nullglob
          shellcheck \
            --shell=bash \
            --severity=warning \
            --exclude=SC1090,SC1091,SC2155 \
            bootstrap.sh \
            env_bootstrap.sh \
            thefiles/.ansible_preauth \
            thefiles/.aliases \
            thefiles/.common_functions \
            thefiles/.exports \
            thefiles/.profile \
            thefiles/.security \
            thefiles/.shell_options \
            thefiles/.scripts/brew_maintain \
            thefiles/.scripts/ansible-vault-pass \
            thefiles/.scripts/agent-sessions \
            scripts/test-agent-sessions.sh

  shell-syntax:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Install zsh
        run: sudo apt-get update && sudo apt-get install -y zsh

      - name: bash -n on bash files
        run: |
          for f in bootstrap.sh env_bootstrap.sh \
                   thefiles/.ansible_preauth \
                   thefiles/.aliases \
                   thefiles/.common_functions \
                   thefiles/.exports \
                   thefiles/.profile \
                   thefiles/.security \
                   thefiles/.shell_options \
                   thefiles/.scripts/brew_maintain \
                   thefiles/.scripts/ansible-vault-pass \
                   thefiles/.scripts/agent-sessions \
                   scripts/test-agent-sessions.sh; do
            bash -n "$f" && echo "ok: $f"
          done

      - name: zsh -n on zsh-only files
        run: |
          for f in thefiles/.zshrc thefiles/.zprofile \
                   thefiles/.zsh_options thefiles/.zsh_keys; do
            zsh -n "$f" && echo "ok: $f"
          done

  agent-sessions:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      # jq and git ship with the runner image; the test needs nothing else.
      - name: Contract test
        run: scripts/test-agent-sessions.sh

  brewfile:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v5
      - name: Validate Brewfile parses
        run: brew bundle list --file=thefiles/Brewfile

  bootstrap-dry-run:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v5
      - name: Symlink dry-run
        run: ./bootstrap.sh --dry-run
